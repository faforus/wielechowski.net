import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./App";
import reportWebVitals from "./reportWebVitals";

// DROPDOWN FADE
// LAZDY LOADING
// FORM SERVER SIDE VALIDATION
// webpSupport into redux? how often does it run
// Burger menu
// CHECK IF USEMODAL ON ITS OWN WORKS
// SCROLL ISSUES APPEARING AND DISAPPEARING DURING ROUTE CHANGES
// 2 / 4 / 6 clicks trigger endless spinner

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

reportWebVitals();
